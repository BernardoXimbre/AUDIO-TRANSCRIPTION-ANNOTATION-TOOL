/**
 * Jest setup file for database connection
 * Runs once before all tests to ensure database is available
 */

module.exports = async () => {
  // Database setup can be added here if needed
  console.log('Setting up test environment...');
};
