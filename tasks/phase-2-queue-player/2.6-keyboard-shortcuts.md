# Task 2.6: Keyboard Shortcuts

## Quick Info
- **Phase**: 2 (Queue & Player)
- **Duration**: 30 minutes
- **Responsible Agent**: Implementation
- **Status**: ⏳ Ready

---

## 📋 Deliverable

**File**: Enhanced `apps/frontend/src/components/Player.vue`

**Purpose**: Add keyboard shortcuts for audio playback control

---

## ⚡ Exactly

Add to Player.vue script:

```typescript
import { onMounted, onUnmounted } from 'vue';

const showHelpModal = ref(false);

function handleKeyPress(event: KeyboardEvent) {
  if (!audioElement.value) return;
  
  switch (event.code) {
    case 'Space':
      event.preventDefault();
      togglePlayPause();
      break;
    case 'ArrowRight':
      event.preventDefault();
      forward();
      break;
    case 'ArrowLeft':
      event.preventDefault();
      rewind();
      break;
    case 'Equal': // Plus
    case 'NumpadAdd':
      event.preventDefault();
      playbackSpeed.value = Math.min(2, playbackSpeed.value + 0.25);
      changeSpeed();
      break;
    case 'Minus': // Minus
    case 'NumpadSubtract':
      event.preventDefault();
      playbackSpeed.value = Math.max(0.75, playbackSpeed.value - 0.25);
      changeSpeed();
      break;
    case 'Slash':
      event.preventDefault();
      showHelpModal.value = !showHelpModal.value;
      break;
  }
}

onMounted(() => {
  window.addEventListener('keydown', handleKeyPress);
});

onUnmounted(() => {
  window.removeEventListener('keydown', handleKeyPress);
});
```

Add to template:

```vue
<!-- Help Modal -->
<div v-if="showHelpModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
  <div class="bg-white p-6 rounded-lg shadow-lg max-w-sm">
    <h2 class="text-lg font-bold mb-4">Keyboard Shortcuts</h2>
    <ul class="text-sm space-y-2">
      <li><kbd class="bg-gray-200 px-2 py-1 rounded">Space</kbd> Play/Pause</li>
      <li><kbd class="bg-gray-200 px-2 py-1 rounded">→</kbd> Skip +1s</li>
      <li><kbd class="bg-gray-200 px-2 py-1 rounded">←</kbd> Skip -1s</li>
      <li><kbd class="bg-gray-200 px-2 py-1 rounded">+</kbd> Speed +0.25x</li>
      <li><kbd class="bg-gray-200 px-2 py-1 rounded">−</kbd> Speed -0.25x</li>
      <li><kbd class="bg-gray-200 px-2 py-1 rounded">?</kbd> Show help</li>
    </ul>
    <button 
      @click="showHelpModal = false"
      class="mt-4 w-full px-3 py-2 bg-blue-500 text-white rounded hover:bg-blue-600"
    >
      Close
    </button>
  </div>
</div>

<!-- Help button -->
<button 
  @click="showHelpModal = true"
  class="text-xs text-blue-500 hover:text-blue-700"
>
  ? Shortcuts
</button>
```

---

## ✅ Review Checklist

- [ ] Space toggles play/pause
- [ ] Arrow right skips +1s
- [ ] Arrow left skips -1s
- [ ] Plus (+) increases speed +0.25x
- [ ] Minus (-) decreases speed -0.25x
- [ ] ? (forward slash) shows/hides help modal
- [ ] Help modal displays all shortcuts
- [ ] No conflicts with browser shortcuts

---

## 🧪 Acceptance Criteria

✅ **Task complete when:**
1. All 6 shortcuts work
2. Help modal displays correctly
3. Keyboard events prevent default where appropriate
4. No global conflicts

---

## 📊 Task Dependencies

\`\`\`
Depends On:      2.4-basic-player, 2.5-player-controls
Blocks:          2.7-recording-conditions
\`\`\`

---

**Next**: [2.7-recording-conditions.md](./2.7-recording-conditions.md)
