# Task 0.2: Express Server Scaffold

## Quick Info
- **Phase**: 0 (Setup)
- **Duration**: 30 minutes
- **Responsible Agent**: Implementation
- **Status**: ⏳ Ready

---

## 📋 Deliverable

**Files**:
- `apps/backend/src/server.ts` - Main Express app
- `apps/backend/src/controllers/` - Controllers folder (empty for now)
- `apps/backend/src/services/` - Services folder (empty for now)
- `apps/backend/src/middleware/` - Middleware folder
- `apps/backend/src/utils/` - Utils folder (empty for now)

---

## ⚡ Exactly

Create `apps/backend/src/server.ts`:

```typescript
import express, { Express, Request, Response } from 'express';
import cors from 'cors';

const app: Express = express();
const PORT = process.env.PORT || 5000;

// Middleware
app.use(cors());
app.use(express.json({ limit: '100mb' }));
app.use(express.urlencoded({ limit: '100mb', extended: true }));

// Health check
app.get('/health', (req: Request, res: Response) => {
  res.status(200).json({ status: 'ok', timestamp: new Date().toISOString() });
});

// Error handling middleware
app.use((err: any, req: Request, res: Response, next: any) => {
  console.error('Error:', err);
  res.status(err.status || 500).json({
    error: err.message || 'Internal server error',
    status: err.status || 500
  });
});

// Start server
app.listen(PORT, () => {
  console.log(`✅ Backend running on http://localhost:${PORT}`);
});

export default app;
```

**Folders to create** (empty for now):
```
apps/backend/src/
├── server.ts          (above)
├── controllers/       (empty)
├── services/          (empty)
├── middleware/        (empty)
├── repositories/      (empty)
└── utils/             (empty)
```

**package.json update**:
```json
{
  "scripts": {
    "dev": "ts-node src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  }
}
```

---

## ✅ Review Checklist

- [ ] `apps/backend/src/server.ts` exists
- [ ] All folders created (controllers, services, middleware, etc.)
- [ ] `yarn dev:backend` starts without errors
- [ ] Browser/curl: `http://localhost:5000/health` returns `{ status: 'ok' }`
- [ ] No TypeScript errors: `yarn tsc --noEmit`

---

## 🧪 Acceptance Criteria

✅ Task complete when:
1. Server starts on port 5000
2. GET /health returns 200 with status: 'ok'
3. Zero TypeScript errors
4. All folders exist (ready for Phase 1)

---

## 🔗 References

- **DESIGN.md** (Section 2): Backend Architecture
- **README.md** (Dev Workflow): Express setup

---

## 📊 Dependencies

```
Depends On:      0.1-prisma-schema
Blocks:          0.4-docker-verification, 1.1-ingest-endpoint
```

---

**Next**: [0.3-vue-app.md](./0.3-vue-app.md)
