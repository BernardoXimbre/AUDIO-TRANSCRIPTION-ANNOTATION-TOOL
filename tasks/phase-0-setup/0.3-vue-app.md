# Task 0.3: Vue App Scaffold

## Quick Info
- **Phase**: 0 (Setup)
- **Duration**: 30 minutes
- **Responsible Agent**: Implementation
- **Status**: ⏳ Ready

---

## 📋 Deliverable

**Files**:
- `apps/frontend/vite.config.ts`
- `apps/frontend/src/main.ts`
- `apps/frontend/src/App.vue`
- `apps/frontend/src/store/` - Pinia store

---

## ⚡ Exactly

Create `apps/frontend/vite.config.ts`:

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true
      }
    }
  }
})
```

Create `apps/frontend/src/main.ts`:

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'

const app = createApp(App)
app.use(createPinia())
app.mount('#app')
```

Create `apps/frontend/src/App.vue`:

```vue
<template>
  <div id="app" class="container">
    <h1>Audio Transcription Annotation Tool</h1>
    <p>✅ Frontend loaded</p>
  </div>
</template>

<script setup lang="ts">
</script>

<style>
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 20px;
}
.container {
  max-width: 1200px;
  margin: 0 auto;
}
</style>
```

Create `apps/frontend/src/store/index.ts`:

```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useMainStore = defineStore('main', () => {
  const appReady = ref(false)
  
  return { appReady }
})
```

**Folders**:
```
apps/frontend/src/
├── main.ts
├── App.vue
├── components/    (empty)
├── store/
│   └── index.ts
└── views/         (empty)
```

**Update package.json**:
```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

---

## ✅ Review Checklist

- [ ] Vite config exists with port 3000 + API proxy
- [ ] Main.ts creates Vue app + Pinia
- [ ] App.vue renders a placeholder
- [ ] `yarn dev:frontend` starts without errors
- [ ] Browser shows "✅ Frontend loaded"
- [ ] Zero TypeScript errors

---

## 🧪 Acceptance Criteria

✅ Task complete when:
1. Frontend dev server starts on port 3000
2. Browser shows placeholder (no errors)
3. Pinia store initialized
4. All folders created

---

## 📊 Dependencies

```
Depends On:      0.2-express-server (independent actually)
Blocks:          0.4-docker-verification
```

---

**Next**: [0.4-docker-verification.md](./0.4-docker-verification.md)
