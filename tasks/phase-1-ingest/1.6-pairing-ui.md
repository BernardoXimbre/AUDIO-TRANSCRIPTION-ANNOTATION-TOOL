# Task 1.6: Pairing UI Component

## Quick Info
- **Phase**: 1 (Ingest)
- **Duration**: 30 minutes
- **Responsible Agent**: Implementation
- **Status**: ⏳ Ready

---

## 📋 Deliverable

**File**: `apps/frontend/src/components/PairingUI.vue`

**Purpose**: Display auto-matched pairs and unmatched items, allow manual pairing via drag-drop or clicks

---

## ⚡ Exactly

Create `apps/frontend/src/components/PairingUI.vue`:

```vue
<template>
  <div class="pairing-container max-w-4xl mx-auto p-6">
    <h1 class="text-3xl font-bold mb-2">Review & Pair Audio with Transcripts</h1>
    <p class="text-gray-600 mb-6">Auto-matched pairs shown below. Drag unmatched items to pair them manually.</p>
    
    <div class="grid grid-cols-2 gap-6">
      <!-- Left Column: Audio Files -->
      <div class="border border-gray-300 rounded-lg p-4">
        <h2 class="text-lg font-semibold mb-4">📁 Audio Files</h2>
        
        <!-- Matched Audio -->
        <div v-if="matchedPairs.length > 0" class="mb-6">
          <h3 class="text-sm font-semibold text-green-700 mb-2">✓ Matched ({{ matchedPairs.length }})</h3>
          <div class="space-y-2">
            <div 
              v-for="pair in matchedPairs"
              :key="pair.audio"
              class="p-2 bg-green-100 border border-green-300 rounded text-sm cursor-move hover:bg-green-150"
              draggable="true"
              @dragstart="startDrag($event, pair.audio, 'audio')"
            >
              {{ pair.audio }} → {{ pair.transcript }}
              <button 
                @click="unpairItem(pair.audio)"
                class="ml-2 text-red-500 hover:text-red-700 text-xs"
              >
                Unpair
              </button>
            </div>
          </div>
        </div>
        
        <!-- Unmatched Audio -->
        <div v-if="unmatchedAudio.length > 0" class="mb-6">
          <h3 class="text-sm font-semibold text-orange-700 mb-2">⚠ Unmatched Audio ({{ unmatchedAudio.length }})</h3>
          <div 
            class="border-2 border-dashed border-orange-300 rounded p-3 min-h-16 bg-orange-50 space-y-1"
            @dragover.prevent="dragOverZone = 'audio'"
            @dragleave.prevent="dragOverZone = null"
            @drop.prevent="handleDropAudio"
            :class="{ 'bg-orange-100 border-orange-500': dragOverZone === 'audio' }"
          >
            <div 
              v-for="audio in unmatchedAudio"
              :key="audio"
              class="p-2 bg-orange-100 border border-orange-300 rounded text-sm cursor-move hover:bg-orange-150"
              draggable="true"
              @dragstart="startDrag($event, audio, 'audio')"
            >
              📄 {{ audio }}
            </div>
            <div v-if="unmatchedAudio.length === 0" class="text-gray-400 text-sm italic p-2">
              Drop unmatched transcripts here
            </div>
          </div>
        </div>
      </div>
      
      <!-- Right Column: Transcripts -->
      <div class="border border-gray-300 rounded-lg p-4">
        <h2 class="text-lg font-semibold mb-4">📝 Transcripts</h2>
        
        <!-- Matched Transcripts -->
        <div v-if="matchedTranscripts.length > 0" class="mb-6">
          <h3 class="text-sm font-semibold text-green-700 mb-2">✓ Matched ({{ matchedTranscripts.length }})</h3>
          <div class="space-y-2">
            <div 
              v-for="transcript in matchedTranscripts"
              :key="transcript"
              class="p-2 bg-green-100 border border-green-300 rounded text-sm truncate"
            >
              {{ transcript }}
            </div>
          </div>
        </div>
        
        <!-- Unmatched Transcripts -->
        <div v-if="unmatchedTranscript.length > 0" class="mb-6">
          <h3 class="text-sm font-semibold text-orange-700 mb-2">⚠ Unmatched Transcripts ({{ unmatchedTranscript.length }})</h3>
          <div 
            class="border-2 border-dashed border-orange-300 rounded p-3 min-h-16 bg-orange-50 space-y-1"
            @dragover.prevent="dragOverZone = 'transcript'"
            @dragleave.prevent="dragOverZone = null"
            @drop.prevent="handleDropTranscript"
            :class="{ 'bg-orange-100 border-orange-500': dragOverZone === 'transcript' }"
          >
            <div 
              v-for="transcript in unmatchedTranscript"
              :key="transcript"
              class="p-2 bg-orange-100 border border-orange-300 rounded text-sm cursor-move hover:bg-orange-150 truncate"
              draggable="true"
              @dragstart="startDrag($event, transcript, 'transcript')"
            >
              📄 {{ transcript }}
            </div>
            <div v-if="unmatchedTranscript.length === 0" class="text-gray-400 text-sm italic p-2">
              Drop unmatched audio files here
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Summary Stats -->
    <div class="mt-8 p-4 bg-blue-50 border border-blue-300 rounded">
      <h3 class="font-semibold mb-2">Summary</h3>
      <ul class="text-sm space-y-1">
        <li>✓ Matched Pairs: {{ matchedPairs.length }}</li>
        <li>⚠ Unmatched Audio: {{ unmatchedAudio.length }}</li>
        <li>⚠ Unmatched Transcripts: {{ unmatchedTranscript.length }}</li>
        <li>📊 Total Audio Files: {{ matchedPairs.length + unmatchedAudio.length }}</li>
        <li>📊 Total Transcripts: {{ matchedPairs.length + unmatchedTranscript.length }}</li>
      </ul>
    </div>
    
    <!-- Action Buttons -->
    <div class="mt-8 flex gap-3 justify-center">
      <button 
        @click="goBack"
        class="px-6 py-3 bg-gray-300 text-gray-700 font-semibold rounded hover:bg-gray-400"
      >
        ← Back to Upload
      </button>
      <button 
        @click="acceptAndIngest"
        :disabled="unmatchedAudio.length > 0 || unmatchedTranscript.length > 0"
        class="px-6 py-3 bg-green-500 text-white font-semibold rounded hover:bg-green-600 disabled:bg-gray-300 disabled:cursor-not-allowed"
      >
        ✓ Accept & Ingest
      </button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue';

interface PairItem {
  audio: string;
  transcript: string;
}

const props = defineProps<{
  initialMatched: PairItem[];
  initialUnmatchedAudio: string[];
  initialUnmatchedTranscript: string[];
}>();

const emit = defineEmits<{
  'ingest-complete': [pairingResult: { matched: PairItem[]; unmatchedAudio: string[]; unmatchedTranscript: string[] }];
  'back': [];
}>();

const matchedPairs = ref<PairItem[]>(props.initialMatched);
const unmatchedAudio = ref<string[]>(props.initialUnmatchedAudio);
const unmatchedTranscript = ref<string[]>(props.initialUnmatchedTranscript);

let draggedItem: { item: string; type: 'audio' | 'transcript' } | null = null;
const dragOverZone = ref<'audio' | 'transcript' | null>(null);

const matchedTranscripts = computed(() => {
  return matchedPairs.value.map(p => p.transcript);
});

function startDrag(event: DragEvent, item: string, type: 'audio' | 'transcript') {
  draggedItem = { item, type };
  if (event.dataTransfer) {
    event.dataTransfer.effectAllowed = 'move';
  }
}

function handleDropAudio() {
  if (!draggedItem || draggedItem.type !== 'transcript') return;
  
  const audioIndex = unmatchedAudio.value.findIndex(a => a); // This is where we'd pair
  if (audioIndex >= 0 && dragOverZone.value === 'audio') {
    // For now, just allow drag-drop zones to work
  }
  draggedItem = null;
}

function handleDropTranscript() {
  if (!draggedItem || draggedItem.type !== 'audio') return;
  
  if (dragOverZone.value === 'transcript') {
    // Drag-drop logic for manual pairing
  }
  draggedItem = null;
}

function unpairItem(audio: string) {
  const pairIndex = matchedPairs.value.findIndex(p => p.audio === audio);
  if (pairIndex >= 0) {
    const pair = matchedPairs.value[pairIndex];
    matchedPairs.value.splice(pairIndex, 1);
    unmatchedAudio.value.push(audio);
    unmatchedTranscript.value.push(pair.transcript);
  }
}

function goBack() {
  emit('back');
}

function acceptAndIngest() {
  if (unmatchedAudio.value.length > 0 || unmatchedTranscript.value.length > 0) {
    alert('All files must be paired before ingesting');
    return;
  }
  
  emit('ingest-complete', {
    matched: matchedPairs.value,
    unmatchedAudio: unmatchedAudio.value,
    unmatchedTranscript: unmatchedTranscript.value
  });
}
</script>

<style scoped>
.pairing-container {
  background: white;
  min-height: 100vh;
}
</style>
```

---

## ✅ Review Checklist

- [ ] Component renders with two-column layout
- [ ] Shows auto-matched pairs with ✓ indicator
- [ ] Shows unmatched audio on left
- [ ] Shows unmatched transcripts on right
- [ ] Drag-drop zones accept drops
- [ ] Unpair button works
- [ ] Summary stats calculate correctly
- [ ] "Accept & Ingest" disabled while unmatched items exist
- [ ] No TypeScript errors

---

## 🧪 Acceptance Criteria

✅ **Task complete when:**
1. Two-column layout displays (audio left, transcripts right)
2. Auto-matched pairs highlighted in green
3. Unmatched items in orange/dashed zones
4. Unpair button reverses auto-match
5. Summary shows counts
6. No server calls yet (pure UI)

---

## 🔗 References

- **DESIGN.md** (Section: Pairing UI): Component spec
- **Task 1.4**: Pairing logic
- **Task 1.7**: Full flow integration

---

## 📝 Notes

- Manual drag-drop pairing skeleton in place (full logic in 1.7)
- Shows unmatched items clearly (prevents silent data loss)
- Foundation for accepting only fully-paired ingests

---

## 📊 Task Dependencies

\`\`\`
Depends On:      1.4-pairing-logic, 1.5-upload-ui
Blocks:          1.7-full-flow
Related Tasks:   Pairing service
\`\`\`

---

**Next**: [1.7-full-flow.md](./1.7-full-flow.md)
